# Frontend Webhook Configuration (REQUIRED)
# This is the endpoint your frontend will call
# 
# OPTION 1 (RECOMMENDED): Use the built-in rate-limited proxy
# Set this to the Netlify function path
VITE_WEBHOOK_URL=/.netlify/functions/submit-comment
# 
# OPTION 2: Direct n8n webhook URL (no rate limiting)
# VITE_WEBHOOK_URL=https://your-n8n-instance.com/webhook/get-comments

# Backend Webhook Configuration (REQUIRED for rate-limited proxy)
# This is your ACTUAL n8n webhook URL that the proxy forwards requests to
# Only needed if using VITE_WEBHOOK_URL=/.netlify/functions/submit-comment
# The webhook should accept POST requests with JSON data containing:
# - comment_url: The LinkedIn comment URL
# - id: The extracted post/comment ID
N8N_WEBHOOK_URL=https://your-n8n-instance.com/webhook/get-comments
